<view class="tabs">
  <view class="tabs_title">
    <view wx:for="{{list}}" wx:for-item="item" wx:key="id" class="title_item {{item.isActive ? 'active' : ''}}" bindtap="itemHandle" data-index="{{index}}">
      {{item.name}}
    </view>
  </view>
  <view class="tabs_content">
    
  </view>
</view>